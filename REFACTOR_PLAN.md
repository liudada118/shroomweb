# 首页交互重构方案

## A. 首页相关文件定位

| 文件 | 职责 | 行数 |
|------|------|------|
| `client/src/pages/Home.tsx` | 首页入口，仅包含 Navbar + HeroSensorSection | 16 |
| `client/src/components/hero/HeroSensorSection.tsx` | 滚动容器（600vh），进度计算，阶段指示器 | 148 |
| `client/src/components/hero/SensorScene.tsx` | **核心**：Three.js 3D 场景，6 屏动画状态机 | ~778 |
| `client/src/components/hero/sensorData.ts` | 真实传感器数据（22帧×32×32） | 26 |
| `client/src/components/hero/SignalHUD.tsx` | 2D Canvas HUD 面板（未接入首页） | 234 |
| `client/src/components/hero/HeroCopy.tsx` | 旧版 8 屏文案层（未接入首页） | - |

## B. 当前首页交互问题诊断

### 1. 清晰度（链路是否可读）— 评分 4/10

- **第5屏（受压）完全空白**：pressHead 和 glowRings 被 `visible=false` 硬隐藏，但阶段名仍显示"05 受压"。用户滚动到这里看到的是静止的布料，没有任何受压表达。
- **第6屏（读出）布料消失**：`prodMat.opacity = clamp01(1 - s6 * 1.8)` 让布料在第6屏快速变透明，违反"产品本体始终可见"的要求。粒子替代了产品，而不是叠加在产品上方。
- **纱线→构网过渡生硬**：双螺旋纱线在 p=0.167~0.25 快速淡出，然后网格纤维从屏幕外滑入。两者之间没有视觉连续性——纱线消失后才出现网格，不像"纱线变成网格"。
- **加工层（第3屏）表达模糊**：sensingLayer 只是一个半透明白色平面从上方缩放落下，看不出"非织造"的材质特征。

### 2. 高级感（视觉品质）— 评分 5/10

- **材质单调**：网格纤维全是灰白色圆柱体，没有纤维质感。产品层靠贴图撑场面。
- **光效缺乏层次**：scanLight 在第1屏和第4屏闪现，但其他屏没有光效变化，导致中间几屏视觉平淡。
- **粒子颜色过于饱和**：jet 色图的蓝/紫色粒子在暗背景上显得"赛博"而非"材料科技"。
- **布料消失时的过渡**：布料变透明后露出的是黑色背景，不是产品仍在下方的感觉。

### 3. 顺滑度（过渡质量）— 评分 6/10

- **滚动缓冲有效**：`displayProgress += (raw - display) * 0.08` 提供了基本的惯性。
- **镜头有6个分段**：c1~c6 各自独立 smoothstep，但相邻段之间有 ~15% 的 p 值重叠，基本连续。
- **第5屏是死区**：p=0.667~0.833 之间没有任何视觉变化，用户滚动了一整屏却什么都没发生。
- **第4屏→第6屏跳跃**：从"布料成型"直接跳到"粒子读出"，中间缺少"受压形变"的过渡。

### 4. 技术表达（是否体现传感器技术）— 评分 3/10

- **缺少局部受压**：这是传感器最核心的技术点——施压后导电路径变化产生信号。当前完全没有表达。
- **数据与产品脱节**：粒子热图覆盖整块布料，但布料在粒子出现时消失了。用户看不到"产品被按压→上方出现数据"的因果关系。
- **数据集中在左下角**：真实数据的非零区域在 rows 15-29, cols 2-16（32×32 网格的左下象限），但当前没有任何视觉引导让用户注意到这个区域。

### 5. 性能风险 — 评分 7/10

- **粒子数量合理**：64×64 = 4096 个点，性能开销小。
- **每帧计算量大**：bilinearInterp(1024→4096) + gaussBlur(4096, 5×5核) + 4096次颜色计算，约 ~50K 次浮点运算/帧。可接受但有优化空间。
- **网格纤维数量多**：32×2 = 64 根独立 Mesh，每根有独立材质。可以合并为 InstancedMesh。
- **无 LOD 或移动端降级**：移动端同样渲染 64 根纤维 + 4096 粒子。

## C. 重构方案（按滚动阶段）

### 新的 6 屏时间线

| 屏 | 进度范围 | 阶段名 | 主动作 | 镜头 |
|----|---------|--------|--------|------|
| 01 | 0~0.167 | 纱线 | 双纱线入场→双螺旋缠绕 | 平视，近距离跟随纱线 |
| 02 | 0.167~0.333 | 构网 | 螺旋淡出，经纬纤维逐根铺出 32×32 网格 | 缓慢拉高，俯视网格展开 |
| 03 | 0.333~0.5 | 加工 | 半透明非织造层从上方覆合到网格上 | 继续拉高，看到完整网格+覆层 |
| 04 | 0.5~0.667 | 成型 | 产品层（带 logo 贴图）出现，网格淡出 | 微侧移，展示产品全貌 |
| 05 | 0.667~0.833 | 受压 | **数据驱动局部形变**：productSheet 和 sensingLayer 的顶点根据 SENSOR_FRAMES 数据产生局部下陷 | 推近受压区域 |
| 06 | 0.833~1.0 | 读出 | **产品保持可见+形变**，粒子热图在产品上方 0.3~0.5 高度生长出来 | 拉高+侧移，45° 俯视 |

### 关键改动

1. **第5屏恢复受压**：用 SENSOR_FRAMES 第6帧（最活跃帧，156个非零点）驱动 productSheet 和 sensingLayer 的顶点 Y 坐标。非零区域在 rows 15-29, cols 2-16，对应布料左下象限。
2. **第6屏产品不消失**：移除 `prodMat.opacity = clamp01(1 - s6 * 1.8)`。产品始终 opacity=1。粒子在产品上方 y=0.3~0.5 处生长。
3. **粒子颜色调整**：将 jet 色图中的蓝色底色改为接近透明/暗灰，只有有数据的区域才亮起。
4. **镜头连续弧线**：保持现有 c1~c6 结构但微调参数，确保第5屏推近受压区域。

## D. 保留/删除/重做清单

| 效果 | 处置 | 原因 |
|------|------|------|
| 01 纱线入场+双螺旋 | **保留** | 效果好，表达清晰 |
| 02 网格逐根铺出 | **保留** | 效果好，有工艺感 |
| 03 非织造层覆合 | **微调** | 缩放动画改为从上方平移落下，更像"覆合" |
| 04 产品层成型 | **保留** | 贴图效果好 |
| 04 网格淡出 | **保留** | 合理的过渡 |
| 05 pressHead 压头 | **删除** | 不需要外部压头，直接用数据驱动形变 |
| 05 glowRings 光环 | **删除** | 喧宾夺主，不服务于技术表达 |
| 06 布料消失 | **删除** | 违反"产品始终可见" |
| 06 粒子热图 | **重做** | 保留粒子系统，但改为产品上方的数据层，不替代产品 |
| scanLight 扫描光 | **微调** | 第5屏受压时用于照亮受压区域 |
| SignalHUD | **暂不接入** | 数据是假的，后续可接入真实数据 |

## E. 两个方案

### 最小改动版本（推荐先实施）

1. 第5屏：用 SENSOR_FRAMES[5]（静态单帧）驱动 productSheet 顶点形变
2. 第6屏：移除布料消失逻辑，粒子 position.y 改为 0.3+高度
3. 粒子：无数据区域改为透明而非蓝色
4. 镜头：第5屏推近受压区域

改动量：~80 行代码

### 理想版本

1. 第5屏：SENSOR_FRAMES 逐帧播放驱动形变（22帧循环）
2. 同一份数据同时驱动：productSheet 形变 + sensingLayer 形变 + 粒子高度/颜色
3. 粒子改为自定义 ShaderMaterial（圆形点+发光边缘）
4. 网格纤维改为 InstancedMesh 提升性能
5. 移动端降级：粒子分辨率减半（32×32），网格纤维减半（16根）

改动量：~200 行代码

## F. 可调参数

| 参数 | 当前值 | 建议值 | 作用 |
|------|--------|--------|------|
| 受压振幅 (pressDepth) | 无 | -0.15 | 顶点下陷最大深度 |
| 数据层高度 (heatmapBaseY) | 0.1 | 0.35 | 粒子层距产品表面的高度 |
| 粒子大小 (pointSize) | 0.065 | 0.055 | 单个粒子的视觉大小 |
| 高度映射比例 (heightScale) | 0.8 | 0.5 | 数据值→粒子高度的映射系数 |
| 帧间隔 (FRAME_INTERVAL) | 0.3s | 0.4s | 数据帧播放速度 |
| 镜头推进速度 | smoothstep 区间 | 可调 | 各阶段镜头过渡的快慢 |
| 无数据粒子透明度 | 0.5(蓝色) | 0.05 | 无压力区域粒子的可见度 |
| 滚动缓冲系数 | 0.08 | 0.06 | 滚动→显示进度的跟随速度 |
